{
  "name": "brewdeck",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Espresso-bar voice console for Claude Code — brew prompts from your phone",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node scripts/test-voice.mjs && node scripts/test-call.mjs && node scripts/test-lifecycle.mjs",
    "test:live": "node scripts/test.mjs"
  },
  "dependencies": {
    "@playwright/mcp": "0.0.80",
    "express": "^4.21.2",
    "qrcode-terminal": "^0.12.0",
    "selfsigned": "^2.4.1",
    "web-push": "^3.6.7",
    "ws": "^8.18.0"
  }
}
