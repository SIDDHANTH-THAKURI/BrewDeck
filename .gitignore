node_modules/

# secrets — PIN + token secret; never commit
brewdeck.config.json
brewdeck.config.json.bak

# secrets — Twilio / Deepgram / ElevenLabs keys
.env

# self-signed TLS private key + cert
.cert/

# persisted brews + push subscriptions
.brews/

# runtime logs
*.log

# test artifacts
qa-shots/

# page snapshots the browser MCP server drops while driving a call
.playwright-mcp/
